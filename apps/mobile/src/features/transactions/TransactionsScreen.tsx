import React from 'react';
import { PhasePlaceholder } from '../../components/PhasePlaceholder';

/** Movimentações realizadas — Fase 4, screenshot 1g-movimentacoes.png. */
export function TransactionsScreen(): React.JSX.Element {
  return (
    <PhasePlaceholder
      title="Movimentações"
      phase="Fase 4 — Movimentações"
      screenshot="design/screenshots/1g-movimentacoes.png"
    />
  );
}
